# coinalyze-receiver v1.00 作業指示書

## 現在地

このブランチは `v1.00` です。

目的は Coinalyze API 専用 Receiver / Collector を安定化し、後段の pseudo footprint renderer が使える normalized data を保存することです。

`btc-receiver` とは別ラインです。Binance WebSocket、板、ローカル orderbook、`live_book_bucketed` には依存しません。

---

## 採用ライブラリ

Coinalyze API client は自前実装ではなく、以下を採用します。

```text
ivarurdalen/coinalyze
```

理由:

- endpoint enum がある
- history endpoint を統一的に扱える
- API key header 処理がある
- 40 calls/min rate limit がある
- retry がある

`pyproject.toml` では Git 依存として指定しています。

```toml
coinalyze @ git+https://github.com/ivarurdalen/coinalyze.git
```

外部SDKの都合により Python は `>=3.11` です。

---

## v1.00 の範囲

### 対象

- Future markets 確認
- OHLCV 1min 取得
- Open Interest history 取得
- Liquidation history 取得
- Funding Rate history 取得
- Long/Short Ratio history 取得
- raw JSONL 保存
- normalized JSONL 保存
- `runtime/health.json` 出力
- `run-once` / `loop`
- 1m OHLCV/bv -> 15m pseudo footprint transform
- 簡易 CVD heatmap render
- Discord webhook notify

### 対象外

- Binance WebSocket
- orderbook heatmap
- tick-level true footprint
- 売買判断
- bot執行
- 本格的な横棒footprint UI

---

## セットアップ

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
export COINALYZE_API_KEY="your_api_key_here"
```

Discord通知を使う場合:

```bash
export DISCORD_WEBHOOK_URL="your_webhook_url_here"
```

API key / webhook URL は保存・表示しないこと。

---

## 基本コマンド

### market確認

```bash
python -m coinalyze_receiver.cli markets --query BTCUSDT
```

### 1回取得

```bash
python -m coinalyze_receiver.cli run-once --symbol BTCUSDT_PERP.A --lookback 6h
```

または:

```bash
bash scripts/run_once.sh BTCUSDT_PERP.A 6h
```

### loop取得

```bash
python -m coinalyze_receiver.cli loop --symbol BTCUSDT_PERP.A --lookback 6h --every 15m
```

### render

```bash
python -m coinalyze_receiver.cli render --price-bucket-usd 10
```

### Discord通知

```bash
python -m coinalyze_receiver.cli notify
```

---

## 出力仕様

raw:

```text
data/raw/{dataset}.jsonl
```

normalized:

```text
data/normalized/{dataset}.jsonl
```

health:

```text
runtime/health.json
```

chart:

```text
data/cvd_heatmap.png
```

---

## OHLCV正規化仕様

Coinalyze OHLCV の `v` と `bv` から以下を作る。

```text
volume      = v
buy_volume  = bv
sell_volume = v - bv
delta       = 2*bv - v
```

---

## Pseudo Footprint 変換仕様

これは true footprint ではありません。

正しい呼称:

```text
Coinalyze OHLCV-based pseudo footprint
pseudo footprint
CVD heatmap proxy
```

変換ルール:

```text
1分足ごとに high-low の価格bucketへ buy/sell を均等配分
→ 15分足へ合算
→ price bucketごとの buy / sell / cvd を生成
```

デフォルト:

```text
表示足: 15分
価格bucket: 10 USD
```

---

## テスト

必ず実行:

```bash
pytest
```

最低限確認すること:

- `normalize_ohlcv`
- `build_pseudo_footprint`
- 1分足単位配分
- 15分集計
- 10ドルbucket
- raw-like row fallback

---

## v1.00 Done Definition

以下を満たせば v1.00 完了。

- `pip install -e .` が通る
- `pytest` が通る
- `markets --query BTCUSDT` が通る
- `run-once` が raw / normalized JSONL を保存する
- `runtime/health.json` が出る
- `render --price-bucket-usd 10` が `data/cvd_heatmap.png` を生成する
- `notify` がWebhook未設定時に安全に失敗する
- API key / webhook URL を保存・表示しない

---

## 残課題

ローカル/Termux側で実API確認が必要。

- `BTCUSDT_PERP.A` が最適symbolか確認
- Coinalyze実レスポンスと normalizer の完全照合
- Discord webhook実送信確認
- Termuxで `pip install -e .` 確認

---

## 指示書運用ルール

このファイル `作業指示書.md` を最新版の作業指示書とする。

更新時は、古い `作業指示書.md` を `old/` に退避してから新しい指示書を置く。

命名例:

```text
old/2026-05-23_v1.00_作業指示書.md
作業指示書.md
```

古い指示書は削除しない。
