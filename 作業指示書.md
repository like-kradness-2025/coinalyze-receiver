# coinalyze-receiver 作業指示書

## 現在地

このリポは Coinalyze API 専用の Receiver / Collector です。

`btc-receiver` とは別ラインです。Binance WebSocket、板、ローカル orderbook、`live_book_bucketed` には依存しません。

現在は **Receiver MVPコード投入済み・実API検証前** の段階です。

---

## 最優先タスク

### 1. 実APIスモークテスト

API key を設定して、実際に Coinalyze API に接続できるか確認します。

```bash
export COINALYZE_API_KEY="your_api_key_here"
python -m coinalyze_receiver.cli markets --query BTCUSDT
python -m coinalyze_receiver.cli run-once --symbol BTCUSDT_PERP.A --lookback 6h
```

確認対象:

```text
data/raw/*.jsonl
data/normalized/*.jsonl
runtime/health.json
```

合格条件:

- `markets` が正常終了する
- `run-once` が正常終了する
- raw JSONL が保存される
- normalized JSONL が保存される
- `runtime/health.json` に `ok: true` が出る

---

## v1.00 の目的

Coinalyze API から以下を取得し、raw / normalized に保存する Receiver を安定化する。

対象:

- OHLCV 1min
- Open Interest history
- Liquidation history
- Funding Rate history
- Long/Short Ratio history

この段階では描画しません。

---

## v1.00 対象外

- Binance WebSocket
- orderbook heatmap
- tick-level true footprint
- 疑似Footprint変換
- PNG描画
- Discord送信
- 売買判断
- bot執行

---

## 実装済みファイル

```text
README.md
pyproject.toml
.gitignore
.env.example
config/default.json

src/coinalyze_receiver/
  __init__.py
  config.py
  timeutil.py
  storage.py
  api.py
  normalize.py
  receiver.py
  cli.py

scripts/run_once.sh

tests/test_normalize.py
```

---

## 現在のCLI

### market確認

```bash
python -m coinalyze_receiver.cli markets --query BTCUSDT
```

### 1回取得

```bash
python -m coinalyze_receiver.cli run-once --symbol BTCUSDT_PERP.A --lookback 6h
```

または:

```bash
bash scripts/run_once.sh BTCUSDT_PERP.A 6h
```

### loop取得

```bash
python -m coinalyze_receiver.cli loop --symbol BTCUSDT_PERP.A --lookback 6h --every 15m
```

---

## 出力仕様

raw:

```text
data/raw/{dataset}.jsonl
```

normalized:

```text
data/normalized/{dataset}.jsonl
```

health:

```text
runtime/health.json
```

---

## OHLCV正規化仕様

Coinalyze OHLCV の `v` と `bv` から以下を作る。

```text
volume      = v
buy_volume  = bv
sell_volume = v - bv
delta       = 2*bv - v
```

この `buy_volume / sell_volume / delta` は、次フェーズの疑似Footprint生成に使う。

---

## 注意点

### 1. 実APIレスポンス未確認

現在の normalizer は以下の形を想定しています。

```text
[
  {
    symbol: ...,
    history: [ ... ]
  }
]
```

実レスポンスが違う場合は `normalize.py` を修正する。

### 2. Symbol未確定

現在のデフォルトは:

```text
BTCUSDT_PERP.A
```

必ず `markets --query BTCUSDT` で正しいsymbolを確認する。

### 3. Issue #1 は閉じるのが早い

Issue #1 は close 済みだが、実態としては以下が未完了。

- 実APIスモークテスト
- 疑似Footprint変換
- hybrid scale renderer
- Discord sender

必要なら Issue を分割して管理する。

---

## 次フェーズ予定

v1.10 以降で実装する。

- 1m OHLCV/bv -> 15m pseudo footprint変換
- 10ドルbucket均等配分
- 15分集計
- hybrid scale 98p cap
- POC表示
- imbalance label
- ABS? proxy
- PNG renderer
- Discord webhook sender

---

## 指示書運用ルール

このファイル `作業指示書.md` を最新版の作業指示書とする。

更新時は、古い `作業指示書.md` を `old/` に退避してから新しい指示書を置く。

命名例:

```text
old/2026-05-23_v1.00_作業指示書.md
作業指示書.md
```

古い指示書は削除しない。
